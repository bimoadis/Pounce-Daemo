/**
 * Narrative placeholder only.
 *
 * Tool ini hanya ada sebagai placeholder product story.
 */

export function pumpLaunchPlaceholder() {
  return {
    tool: "pump_launch",
    active: false,
    note: "Landing page copy only.",
  };
}
