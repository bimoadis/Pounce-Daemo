{
  "name": "pump-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "MCP server for pump.fun with landing page",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspace @pump-mcp/web",
    "dev": "npm run dev --workspace @pump-mcp/web",
    "start": "npm run preview --workspace @pump-mcp/web"
  }
}
