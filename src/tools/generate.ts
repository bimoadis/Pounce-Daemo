/**
 * Narrative placeholder only.
 *
 * Implementasi generate yang benar-benar aktif:
 * `web/server/generate-token.js`
 */

export function pumpGeneratePlaceholder() {
  return {
    tool: "pump_generate",
    active: false,
    delegatedTo: "web/server/generate-token.js",
  };
}
